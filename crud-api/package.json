{
 "scripts": {
   "dev": "podman compose -f docker-compose.dev.yml up --build",
   "down": "podman compose -f docker-compose.dev.yml down",
   "serve": "podman compose -f docker-compose.prod.yml up --build"
 } 
}